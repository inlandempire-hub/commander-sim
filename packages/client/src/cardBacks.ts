/**
 * The two card backs, served from the client's public folder.
 *
 * They are not in the repo - they are Wizards' artwork, and the project's rule
 * is that none of that is committed (CLAUDE.md, "Scope & non-goals"). Drop your
 * own copies at these paths; docs/SETUP.md says so too. Missing is a supported
 * state, not a broken one: everything that draws a back has a drawn fallback
 * underneath it.
 *
 * Near is yours, far is everybody else's - the two decks are told apart by
 * their backs the way two real players' sleeves are.
 */
export const CARD_BACK_NEAR = "/card-backs/light.png";
export const CARD_BACK_FAR = "/card-backs/dark.png";
